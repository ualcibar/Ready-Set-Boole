#ifndef ADDER_HPP
#define ADDER_HPP

#include <iostream>
#include <cstdint>

// Suma a + b usando SOLO: & | ^ << >> = == != < > <= >=
// (++ o += 1 solo permitido para el índice de un bucle, no para el resultado)
uint32_t adder(uint32_t a, uint32_t b);
uint32_t adder2bit(uint32_t a, uint32_t b);

#endif
